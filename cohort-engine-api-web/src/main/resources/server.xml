<server description="cohort-server">

	<!-- Enable features -->
	<featureManager>
		<feature>transportSecurity-1.0</feature>
		<feature>appSecurity-2.0</feature>
		<feature>jaxrs-2.1</feature>
		<feature>servlet-4.0</feature>
		<feature>jsp-2.3</feature>
		
	<!-- <feature>jsonp-1.1</feature> -->
	<!-- the db2 jcc driver for 11.5.0.0 doesn't fully implement jdbc-4.2 -->
	<!-- feature>jdbc-4.1</feature -->>
	<!-- <feature>websocket-1.1</feature> -->
		
		 <feature>localConnector-1.0</feature>
	<!-- <feature>mpOpenAPI-1.0</feature> -->
	<!-- mpJwt-1.1 isn't used by default,
		but we include it here to avoid NoClassDefFound in our classes that *can* use it -->
	<!--<feature>mpJwt-1.1</feature> -->
	<!--<feature>ssl-1.0</feature> -->
	</featureManager>
	
	<!-- Disable welcome page so that internal server info won't be revealed in responses
		to requests with an invalid context root.
	-->
	<httpDispatcher enableWelcomePage="false" />
	<!-- Disable X-Powered-By header to avoid leaking information and override 
		the default error pages to avoid showing stack traces for nonsensical queries 
		like requests for a JSP page that doesn't exist. -->
	<webContainer disableXPoweredBy="true"
		displayTextWhenNoErrorPageDefined="Unexpected request/response. Please check the URL and try again."
		deferServletLoad="false" />


	<!-- <ssl id="defaultSSLConfig" sslProtocol="TLSv1.2"/> <keyStore id="defaultKeyStore" 
		password="abc123"/> -->

	<!-- IBM FHIR Server's keystore and truststore configuration -->
	<!-- <ssl id="defaultSSLConfig" keyStoreRef="defaultKeyStore" trustStoreRef="defaultTrustStore" 
		clientAuthenticationSupported="true" sslProtocol="TLSv1.2"/> -->
	<!-- <keyStore id="defaultKeyStore" location="${server.output.dir}/resources/security/fhirKeyStore.p12" 
		password="change-password"/> -->
	<!-- <keyStore id="defaultTrustStore" location="${server.output.dir}/resources/security/fhirTrustStore.p12" 
		password="change-password"/> -->

	<!-- To access this server from a remote client add a host attribute to 
		the following element, e.g. host="*" -->
	<httpEndpoint httpPort="9080" httpsPort="9443"
		id="defaultHttpEndpoint" />

	<!-- Automatically expand WAR files and EAR files -->
	<applicationManager autoExpand="true" />

	<applicationMonitor dropinsEnabled="false"
		updateTrigger="mbean" />

	<webApplication id="cohort-engine-api-web"
		location="cohort-engine-api-web-0.0.1-SNAPSHOT.war"
		name="cohort-engine-api-web" />


	<logging consoleLogLevel="INFO" maxFileSize="20" maxFiles="10"
		traceSpecification="" />

</server>