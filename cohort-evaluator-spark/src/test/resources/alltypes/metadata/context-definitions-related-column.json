{
	"contextDefinitions": [{
		"name": "Patient",
		"primaryDataType": "A",
		"primaryKeyColumn": "id_col",
		"relationships": [{
			"name": "B",
			"joinClause": "inner join B on A.id_col = B.string"
		},
		{
			"name": "C",
			"joinClause": "inner join C on A.pat_id = C.pat_id"
		},
		{
			"name": "D",
			"joinClause": "inner join D on D.pat_id = A.pat_id"
		}]
	}]
}